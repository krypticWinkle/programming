to check if there is a digit in a string. (For ex: string x = (a+b)/d*e)
we use:

if(x[i] >='0' && x[i] <= '9')

Similarly, 
if(x[i] >='a' && x[i] <= 'z')
similarly,
if(x[i] >='A' && x[i] <= 'Z')

If you add empty string to a char, it becomes a string. Eg:
string str = ch + "";

If you want to convert a char or a part of a string into int
array[i] - '0'